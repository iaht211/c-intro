#include<stdio.h>
#include<stdlib.h>
#include<time.h>
//ham sinh so ngau nhien khong trung nhau 
int *sinh_so(int size)
{
    srand(time(NULL));
    int *A = (int *)malloc(sizeof(int)*size);
    for(int i=0;i<size;i++)
    {
        

    }
}